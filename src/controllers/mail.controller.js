export const mailController = {
    // get mail templates
    getMailTemplate: (req, res) => {
        res.render('mail');
    },

    // save mail template
    updateMailTemplate: (req, res) => {
        
    }
};